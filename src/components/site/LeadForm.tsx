import {useState,type FormEvent} from "react";import {Button} from "@/components/ui/button";import {Input} from "@/components/ui/input";import {whatsappUrl,allServices} from "@/content/site";
export function LeadForm({compact=false,defaultService=""}:{compact?:boolean;defaultService?:string}){const [error,setError]=useState(""),[submitting,setSubmitting]=useState(false);function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();if(submitting)return;const d=new FormData(e.currentTarget),name=String(d.get("name")||"").trim(),phone=String(d.get("phone")||"").replace(/\D/g,""),city=String(d.get("city")||"").trim(),service=String(d.get("service")||defaultService);if(name.length<2||phone.length!==10||city.length<2){setError("Please enter a valid name, 10-digit phone number and city.");return}setError("");setSubmitting(true);window.open(whatsappUrl(`Hello FININSTA, I am ${name} from ${city}. I am interested in ${service}. My phone number is ${phone}.`),"_blank","noopener,noreferrer");window.setTimeout(()=>setSubmitting(false),1200)};return <form onSubmit={submit} className={compact?"grid gap-4":"grid gap-6 rounded-lg border bg-card p-6 shadow-soft"} noValidate><div className="grid gap-4 sm:grid-cols-2"><label className="field-label">Full name<Input name="name" autoComplete="name" placeholder="Your name" maxLength={80} required/></label><label className="field-label">Phone number<Input name="phone" type="tel" autoComplete="tel" placeholder="10-digit number" inputMode="numeric" pattern="[0-9]{10}" maxLength={10} required/></label></div><div className="grid gap-4 sm:grid-cols-2"><label className="field-label">City<Input name="city" autoComplete="address-level2" placeholder="Your city" maxLength={80} required/></label><label className="field-label">Service<select name="service" defaultValue={defaultService} className="h-[52px] rounded-[14px] border border-input bg-background px-4 text-base focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring" required><option value="" disabled>Select service</option>{allServices.map(s=><option key={s.slug}>{s.title}</option>)}</select></label></div>{error&&<p role="alert" className="text-sm text-destructive">{error}</p>}<Button type="submit" className="gold-hover sm:w-fit" disabled={submitting}>{submitting?"Opening WhatsApp…":"Continue on WhatsApp"}</Button><p className="text-xs text-muted-foreground">By continuing, you consent to be contacted about your enquiry. Approval and rates remain subject to lender policy.</p></form>}

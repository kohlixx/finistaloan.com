import { ArrowRight, CheckCircle2, MessageCircle } from "lucide-react";
import { Button } from "@/components/ui/button";
import { whatsappUrl } from "@/content/site";

export function PageHero({eyebrow,title,description,image,cta="Apply Now on WhatsApp"}:{eyebrow:string;title:string;description:string;image?:string;cta?:string}) {
  return <section className="relative overflow-hidden bg-ink text-ink-foreground"><div className="container-wide grid min-h-[560px] items-center gap-12 py-16 lg:grid-cols-[1fr_.88fr] lg:py-24"><div className="relative z-10"><span className="eyebrow text-gold">{eyebrow}</span><h1 className="mt-4 max-w-3xl text-balance font-heading text-5xl font-bold leading-[1.12]">{title}</h1><p className="mt-6 max-w-2xl text-base leading-8 text-ink-muted">{description}</p><div className="mt-8 flex flex-col gap-4 sm:flex-row"><Button asChild className="gold-hover"><a href={whatsappUrl(`I would like help with ${title}.`)} target="_blank" rel="noreferrer"><MessageCircle/>{cta}</a></Button><Button asChild variant="outline" className="border-ink-line text-ink-foreground hover:bg-ink-soft hover:text-ink-foreground"><a href="#details">Explore details <ArrowRight/></a></Button></div><div className="mt-8 flex flex-wrap gap-x-6 gap-y-2 text-sm text-ink-muted">{["Free consultation","Digital assistance","PAN India support"].map(x=><span key={x} className="inline-flex items-center gap-2"><CheckCircle2 className="size-4 text-gold"/>{x}</span>)}</div></div>{image&&<div className="relative"><img src={image} alt="FININSTA financial consultation" width="1400" height="900" loading="eager" fetchPriority="high" className="aspect-[4/3] w-full rounded-lg object-cover shadow-2xl"/><div className="absolute -bottom-4 left-4 right-4 rounded-lg border border-ink-line bg-ink/85 p-4 backdrop-blur"><b>Finance made simple.</b><span className="mt-1 block text-sm text-ink-muted">One expert. Clear steps. Trusted lender coordination.</span></div></div>}</div></section>
}
